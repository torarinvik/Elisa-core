# include "../frontend_llcontext/contextlang_frontend.llcontext"


global mutable frontend_lexer_bench_arena: Arena = zeroed

const FRONTEND_TOKEN_HASH_OFFSET = 1469598103934665603
const FRONTEND_TOKEN_HASH_PRIME = 1099511628211


def frontend_expect_token_count(tokens: darray[FrontendToken], expected: usize) -> bool:
    return tokens.count == expected


def frontend_kind_eq(actual: FrontendTokenKind, expected: FrontendTokenKind) -> bool:
    return actual == expected


def frontend_expect_kind(tokens: darray[FrontendToken], index: usize, expected: FrontendTokenKind) -> bool:
    if index >= tokens.count:
        return false
    return frontend_kind_eq(tokens[index].kind, expected)


def frontend_expect_text(tokens: darray[FrontendToken], index: usize, expected: static u8&) -> bool:
    if index >= tokens.count:
        return false
    return tokens[index].text == expected


def frontend_expect_suffix(tokens: darray[FrontendToken], index: usize, expected: static u8&) -> bool:
    if index >= tokens.count:
        return false
    return tokens[index].suffix == expected


def frontend_token_kind_code(kind: FrontendTokenKind) -> u64:
    match kind:
        FrontendTokenKind.Eof:
            return 1
        FrontendTokenKind.Newline:
            return 2
        FrontendTokenKind.Indent:
            return 3
        FrontendTokenKind.Dedent:
            return 4
        FrontendTokenKind.Ident:
            return 5
        FrontendTokenKind.IntLit:
            return 6
        FrontendTokenKind.FloatLit:
            return 7
        FrontendTokenKind.HexLit:
            return 8
        FrontendTokenKind.StringLit:
            return 9
        FrontendTokenKind.Def:
            return 10
        FrontendTokenKind.Error:
            return 11
        FrontendTokenKind.Enum:
            return 12
        FrontendTokenKind.Struct:
            return 13
        FrontendTokenKind.Const:
            return 14
        FrontendTokenKind.Global:
            return 15
        FrontendTokenKind.Extern:
            return 16
        FrontendTokenKind.Export:
            return 17
        FrontendTokenKind.If:
            return 18
        FrontendTokenKind.Match:
            return 19
        FrontendTokenKind.Elif:
            return 20
        FrontendTokenKind.Else:
            return 21
        FrontendTokenKind.While:
            return 22
        FrontendTokenKind.Return:
            return 23
        FrontendTokenKind.Any:
            return 24
        FrontendTokenKind.Heap:
            return 25
        FrontendTokenKind.Stack:
            return 26
        FrontendTokenKind.Static:
            return 27
        FrontendTokenKind.Mutable:
            return 28
        FrontendTokenKind.Repr:
            return 29
        FrontendTokenKind.Packed:
            return 30
        FrontendTokenKind.Aligned:
            return 31
        FrontendTokenKind.Pass:
            return 32
        FrontendTokenKind.Panic:
            return 33
        FrontendTokenKind.Not:
            return 34
        FrontendTokenKind.And:
            return 35
        FrontendTokenKind.Or:
            return 36
        FrontendTokenKind.Null:
            return 37
        FrontendTokenKind.True:
            return 38
        FrontendTokenKind.False:
            return 39
        FrontendTokenKind.Zeroed:
            return 40
        FrontendTokenKind.Sizeof:
            return 41
        FrontendTokenKind.Tail:
            return 42
        FrontendTokenKind.Try:
            return 43
        FrontendTokenKind.Raise:
            return 44
        FrontendTokenKind.Region:
            return 45
        FrontendTokenKind.Destroy:
            return 46
        FrontendTokenKind.New:
            return 47
        FrontendTokenKind.As:
            return 48
        FrontendTokenKind.In:
            return 49
        FrontendTokenKind.With:
            return 50
        FrontendTokenKind.Context:
            return 51
        FrontendTokenKind.Colon:
            return 52
        FrontendTokenKind.Arrow:
            return 53
        FrontendTokenKind.Dot:
            return 54
        FrontendTokenKind.Range:
            return 55
        FrontendTokenKind.RangeLt:
            return 56
        FrontendTokenKind.RangeGt:
            return 57
        FrontendTokenKind.Comma:
            return 58
        FrontendTokenKind.LParen:
            return 59
        FrontendTokenKind.RParen:
            return 60
        FrontendTokenKind.LBracket:
            return 61
        FrontendTokenKind.RBracket:
            return 62
        FrontendTokenKind.Hash:
            return 63
        FrontendTokenKind.Ampersand:
            return 64
        FrontendTokenKind.Question:
            return 65
        FrontendTokenKind.QAssign:
            return 66
        FrontendTokenKind.Bang:
            return 67
        FrontendTokenKind.At:
            return 68
        FrontendTokenKind.Ellipsis:
            return 69
        FrontendTokenKind.LArrow:
            return 70
        FrontendTokenKind.PlusEq:
            return 71
        FrontendTokenKind.MinusEq:
            return 72
        FrontendTokenKind.StarEq:
            return 73
        FrontendTokenKind.SlashEq:
            return 74
        FrontendTokenKind.PercentEq:
            return 75
        FrontendTokenKind.CaretEq:
            return 76
        FrontendTokenKind.PipeEq:
            return 77
        FrontendTokenKind.AmpEq:
            return 78
        FrontendTokenKind.LShiftEq:
            return 79
        FrontendTokenKind.RShiftEq:
            return 80
        FrontendTokenKind.Assign:
            return 81
        FrontendTokenKind.EqEq:
            return 82
        FrontendTokenKind.BangEq:
            return 83
        FrontendTokenKind.Lt:
            return 84
        FrontendTokenKind.Gt:
            return 85
        FrontendTokenKind.LtEq:
            return 86
        FrontendTokenKind.GtEq:
            return 87
        FrontendTokenKind.Plus:
            return 88
        FrontendTokenKind.Minus:
            return 89
        FrontendTokenKind.Star:
            return 90
        FrontendTokenKind.Slash:
            return 91
        FrontendTokenKind.Percent:
            return 92
        FrontendTokenKind.Pipe:
            return 93
        FrontendTokenKind.Caret:
            return 94
        FrontendTokenKind.LShift:
            return 95
        FrontendTokenKind.RShift:
            return 96
        FrontendTokenKind.Tilde:
            return 97
        FrontendTokenKind.CharLit:
            return 98
        FrontendTokenKind.Is:
            return 99
        FrontendTokenKind.FatArrow:
            return 100
        FrontendTokenKind.To:
            return 101
    return 0


def frontend_case_char_literals() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[26] = [39, 97, 39, 32, 39, 92, 110, 39, 32, 39, 92, 120, 52, 49, 39, 32, 39, 92, 117, 48, 48, 52, 49, 39, 10, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        if not frontend_expect_token_count(tokens, 6):
            return false
        return frontend_expect_kind(tokens, 0, FrontendTokenKind.CharLit) and frontend_expect_kind(tokens, 1, FrontendTokenKind.CharLit) and frontend_expect_kind(tokens, 2, FrontendTokenKind.CharLit) and frontend_expect_kind(tokens, 3, FrontendTokenKind.CharLit) and frontend_expect_kind(tokens, 4, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 5, FrontendTokenKind.Eof)


def frontend_token_hash_mix(hash: u64, value: u64) -> u64:
    return (hash ^ value) * FRONTEND_TOKEN_HASH_PRIME


def frontend_hash_token(hash: u64, token: FrontendToken) -> u64:
    return frontend_token_hash_mix(hash, frontend_token_kind_code(token.kind))


def frontend_token_checksum(tokens: darray[FrontendToken]) -> u64:
    hash: mutable u64 = frontend_token_hash_mix(FRONTEND_TOKEN_HASH_OFFSET, tokens.count.u64())
    for i in 0..<tokens.count:
        hash <- frontend_hash_token(hash, tokens[i])
    return hash


def frontend_tokenize_test_source(a: mutable any Arena&, source: any u8&) -> darray[FrontendToken] can[Memory.Allocate, Abort.Panic]:
    tokens: darray[FrontendToken] = frontend_tokenize(a, "frontend_lexer_test.llcontext".cast[any u8&], source) can Memory.Allocate, Abort.Panic
    return tokens


def frontend_expect_lookup_keyword(text: static u8&, expected: FrontendTokenKind) -> bool:
    return frontend_kind_eq(frontend_lookup_keyword(string_view(text.cast[any u8&], 0, runtime_strlen(text.cast[any u8&]).i64())), expected)


def frontend_expect_lookup_keyword_entries(entries: any FrontendKeywordEntry&, count: usize) -> bool:
    for i in 0..<count:
        entry: FrontendKeywordEntry = entries[i]
        if not frontend_expect_lookup_keyword(entry.text, entry.kind):
            return false
    return true


def frontend_case_simple_ident() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[7] = [104, 101, 108, 108, 111, 10, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        if not frontend_expect_token_count(tokens, 3):
            return false
        if not frontend_expect_kind(tokens, 0, FrontendTokenKind.Ident):
            return false
        if not frontend_expect_kind(tokens, 1, FrontendTokenKind.Newline):
            return false
        if not frontend_expect_kind(tokens, 2, FrontendTokenKind.Eof):
            return false
        if 0 >= tokens.count:
            return false
        return frontend_sv_eq_literal(tokens[0].text, "hello")


def frontend_case_arrow_ops() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[14] = [120, 32, 60, 45, 32, 121, 32, 45, 62, 32, 122, 10, 0, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        if not frontend_expect_token_count(tokens, 7):
            return false
        return frontend_expect_kind(tokens, 0, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 1, FrontendTokenKind.LArrow) and frontend_expect_kind(tokens, 2, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 3, FrontendTokenKind.Arrow) and frontend_expect_kind(tokens, 4, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 5, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 6, FrontendTokenKind.Eof)


def frontend_case_number_literals() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[25] = [52, 50, 32, 48, 120, 102, 102, 32, 49, 50, 51, 117, 54, 52, 32, 48, 120, 65, 66, 105, 51, 50, 10, 0, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        if not frontend_expect_token_count(tokens, 6):
            return false
        if not frontend_expect_kind(tokens, 0, FrontendTokenKind.IntLit):
            return false
        if not frontend_expect_kind(tokens, 1, FrontendTokenKind.HexLit):
            return false
        if not frontend_expect_kind(tokens, 2, FrontendTokenKind.IntLit):
            return false
        if not frontend_expect_kind(tokens, 3, FrontendTokenKind.HexLit):
            return false
        if not frontend_expect_kind(tokens, 4, FrontendTokenKind.Newline):
            return false
        if not frontend_expect_kind(tokens, 5, FrontendTokenKind.Eof):
            return false
        if 2 >= tokens.count:
            return false
        if not frontend_sv_eq_literal(tokens[2].suffix, "u64"):
            return false
        if 3 >= tokens.count:
            return false
        return frontend_sv_eq_literal(tokens[3].suffix, "i32")


def frontend_case_indent_dedent() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[39] = [100, 101, 102, 32, 102, 111, 111, 58, 10, 32, 32, 32, 32, 120, 32, 60, 45, 32, 49, 10, 32, 32, 32, 32, 121, 32, 60, 45, 32, 50, 10, 122, 32, 60, 45, 32, 51, 10, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        if not frontend_expect_token_count(tokens, 19):
            return false
        return frontend_expect_kind(tokens, 0, FrontendTokenKind.Def) and frontend_expect_kind(tokens, 1, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 2, FrontendTokenKind.Colon) and frontend_expect_kind(tokens, 3, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 4, FrontendTokenKind.Indent) and frontend_expect_kind(tokens, 9, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 13, FrontendTokenKind.Dedent) and frontend_expect_kind(tokens, 18, FrontendTokenKind.Eof)


def frontend_case_comment_and_ellipsis() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[9] = [35, 120, 10, 46, 46, 46, 10, 0, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        if not frontend_expect_token_count(tokens, 3):
            return false
        return frontend_expect_kind(tokens, 0, FrontendTokenKind.Ellipsis) and frontend_expect_kind(tokens, 1, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 2, FrontendTokenKind.Eof)


def frontend_case_shift_ops() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[13] = [120, 32, 60, 60, 32, 51, 32, 62, 62, 32, 50, 10, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        if not frontend_expect_token_count(tokens, 7):
            return false
        return frontend_expect_kind(tokens, 0, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 1, FrontendTokenKind.LShift) and frontend_expect_kind(tokens, 2, FrontendTokenKind.IntLit) and frontend_expect_kind(tokens, 3, FrontendTokenKind.RShift) and frontend_expect_kind(tokens, 4, FrontendTokenKind.IntLit) and frontend_expect_kind(tokens, 5, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 6, FrontendTokenKind.Eof)


def frontend_case_paren_suppresses_newline() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[15] = [102, 111, 111, 40, 97, 44, 10, 32, 32, 32, 32, 98, 41, 10, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        if not frontend_expect_token_count(tokens, 8):
            return false
        return frontend_expect_kind(tokens, 0, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 1, FrontendTokenKind.LParen) and frontend_expect_kind(tokens, 2, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 3, FrontendTokenKind.Comma) and frontend_expect_kind(tokens, 4, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 5, FrontendTokenKind.RParen) and frontend_expect_kind(tokens, 6, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 7, FrontendTokenKind.Eof)


def frontend_case_region_alloc_syntax() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[59] = [114, 101, 103, 105, 111, 110, 32, 112, 97, 114, 115, 101, 10, 118, 97, 108, 117, 101, 58, 32, 97, 110, 121, 32, 105, 51, 50, 38, 32, 61, 32, 110, 101, 119, 91, 112, 97, 114, 115, 101, 93, 32, 49, 10, 100, 101, 115, 116, 114, 111, 121, 32, 112, 97, 114, 115, 101, 10, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        ok: bool = frontend_expect_token_count(tokens, 19) and frontend_expect_kind(tokens, 0, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 1, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 2, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 3, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 4, FrontendTokenKind.Colon) and frontend_expect_kind(tokens, 5, FrontendTokenKind.Any) and frontend_expect_kind(tokens, 6, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 7, FrontendTokenKind.Ampersand) and frontend_expect_kind(tokens, 8, FrontendTokenKind.Assign) and frontend_expect_kind(tokens, 9, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 10, FrontendTokenKind.LBracket) and frontend_expect_kind(tokens, 11, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 12, FrontendTokenKind.RBracket) and frontend_expect_kind(tokens, 13, FrontendTokenKind.IntLit) and frontend_expect_kind(tokens, 14, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 15, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 16, FrontendTokenKind.Ident) and frontend_expect_kind(tokens, 17, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 18, FrontendTokenKind.Eof)
        destroy scratch
        return ok


def frontend_case_with_and_context_keywords() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[14] = [119, 105, 116, 104, 32, 99, 111, 110, 116, 101, 120, 116, 10, 0]
        tokens: darray[FrontendToken] = frontend_tokenize_test_source(a, (&bytes[0]).cast[any u8&])
        ok: bool = frontend_expect_token_count(tokens, 4) and frontend_expect_kind(tokens, 0, FrontendTokenKind.With) and frontend_expect_kind(tokens, 1, FrontendTokenKind.Context) and frontend_expect_kind(tokens, 2, FrontendTokenKind.Newline) and frontend_expect_kind(tokens, 3, FrontendTokenKind.Eof)
        destroy scratch
        return ok


def frontend_case_lookup_keyword_table() -> bool:
    entries: FrontendKeywordEntry[45] = [
        FrontendKeywordEntry("as", FrontendTokenKind.As),
        FrontendKeywordEntry("if", FrontendTokenKind.If),
        FrontendKeywordEntry("in", FrontendTokenKind.In),
        FrontendKeywordEntry("or", FrontendTokenKind.Or),
        FrontendKeywordEntry("to", FrontendTokenKind.To),
        FrontendKeywordEntry("and", FrontendTokenKind.And),
        FrontendKeywordEntry("any", FrontendTokenKind.Any),
        FrontendKeywordEntry("def", FrontendTokenKind.Def),
        FrontendKeywordEntry("not", FrontendTokenKind.Not),
        FrontendKeywordEntry("try", FrontendTokenKind.Try),
        FrontendKeywordEntry("elif", FrontendTokenKind.Elif),
        FrontendKeywordEntry("else", FrontendTokenKind.Else),
        FrontendKeywordEntry("enum", FrontendTokenKind.Enum),
        FrontendKeywordEntry("heap", FrontendTokenKind.Heap),
        FrontendKeywordEntry("null", FrontendTokenKind.Null),
        FrontendKeywordEntry("pass", FrontendTokenKind.Pass),
        FrontendKeywordEntry("repr", FrontendTokenKind.Repr),
        FrontendKeywordEntry("tail", FrontendTokenKind.Tail),
        FrontendKeywordEntry("true", FrontendTokenKind.True),
        FrontendKeywordEntry("with", FrontendTokenKind.With),
        FrontendKeywordEntry("const", FrontendTokenKind.Const),
        FrontendKeywordEntry("error", FrontendTokenKind.Error),
        FrontendKeywordEntry("false", FrontendTokenKind.False),
        FrontendKeywordEntry("match", FrontendTokenKind.Match),
        FrontendKeywordEntry("panic", FrontendTokenKind.Panic),
        FrontendKeywordEntry("raise", FrontendTokenKind.Raise),
        FrontendKeywordEntry("stack", FrontendTokenKind.Stack),
        FrontendKeywordEntry("while", FrontendTokenKind.While),
        FrontendKeywordEntry("export", FrontendTokenKind.Export),
        FrontendKeywordEntry("extern", FrontendTokenKind.Extern),
        FrontendKeywordEntry("global", FrontendTokenKind.Global),
        FrontendKeywordEntry("packed", FrontendTokenKind.Packed),
        FrontendKeywordEntry("return", FrontendTokenKind.Return),
        FrontendKeywordEntry("sizeof", FrontendTokenKind.Sizeof),
        FrontendKeywordEntry("static", FrontendTokenKind.Static),
        FrontendKeywordEntry("struct", FrontendTokenKind.Struct),
        FrontendKeywordEntry("zeroed", FrontendTokenKind.Zeroed),
        FrontendKeywordEntry("aligned", FrontendTokenKind.Aligned),
        FrontendKeywordEntry("context", FrontendTokenKind.Context),
        FrontendKeywordEntry("mutable", FrontendTokenKind.Mutable),
        FrontendKeywordEntry("region", FrontendTokenKind.Ident),
        FrontendKeywordEntry("new", FrontendTokenKind.Ident),
        FrontendKeywordEntry("destroy", FrontendTokenKind.Ident),
        FrontendKeywordEntry("defin", FrontendTokenKind.Ident),
        FrontendKeywordEntry("truer", FrontendTokenKind.Ident)
    ]
    return frontend_expect_lookup_keyword_entries((&entries[0]).cast[any FrontendKeywordEntry&], 45)


def frontend_case_kind_only_keyword_path() -> bool can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        region scratch(4096)
        a: mutable any Arena& = (&scratch).cast[mutable any Arena&]
        bytes: u8[66] = [102, 97, 108, 115, 101, 32, 116, 114, 117, 101, 32, 122, 101, 114, 111, 101, 100, 32, 119, 105, 116, 104, 32, 99, 111, 110, 116, 101, 120, 116, 32, 114, 101, 103, 105, 111, 110, 32, 110, 101, 119, 32, 100, 101, 115, 116, 114, 111, 121, 32, 49, 50, 51, 117, 54, 52, 32, 48, 120, 65, 66, 105, 51, 50, 10, 0]
        lexer: mutable FrontendLexer = frontend_new_lexer(a, "frontend_kind_only_test.llcontext".cast[any u8&], (&bytes[0]).cast[any u8&])

        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.False):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.True):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.Zeroed):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.With):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.Context):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.Ident):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.Ident):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.Ident):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.IntLit):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.HexLit):
            return false
        if not frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.Newline):
            return false
        return frontend_kind_eq(frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&]), FrontendTokenKind.Eof)


def frontend_lexer_parity_suite_impl() -> int can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        if not frontend_case_simple_ident():
            return 0
        if not frontend_case_arrow_ops():
            return 0
        if not frontend_case_number_literals():
            return 0
        if not frontend_case_indent_dedent():
            return 0
        if not frontend_case_comment_and_ellipsis():
            return 0
        if not frontend_case_shift_ops():
            return 0
        if not frontend_case_paren_suppresses_newline():
            return 0
        if not frontend_case_region_alloc_syntax():
            return 0
        if not frontend_case_with_and_context_keywords():
            return 0
        if not frontend_case_lookup_keyword_table():
            return 0
        if not frontend_case_kind_only_keyword_path():
            return 0
        if not frontend_case_char_literals():
            return 0
        return 1


def frontend_count_tokens(a: mutable any Arena&, filename: any u8&, source: any u8&) -> usize can[Memory.Allocate, Abort.Panic]:
    count: usize = frontend_count_tokens_with_len(a, filename, source, runtime_strlen(source)) can Memory.Allocate, Abort.Panic
    return count


def frontend_count_tokens_with_len(a: mutable any Arena&, filename: any u8&, source: any u8&, source_len: usize) -> usize can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        lexer: mutable FrontendLexer = frontend_new_lexer_with_len(a, filename, source, source_len)
        count: mutable usize = 0

        while true:
            kind: FrontendTokenKind = frontend_next_token_kind((&lexer).cast[mutable any FrontendLexer&])
            count <- count + 1
            match kind:
                FrontendTokenKind.Eof:
                    return count

        return count


def frontend_lexer_token_count_impl(source: any u8&) -> i64 can[Memory.Allocate, Abort.Panic]:
    count: i64 = frontend_lexer_token_count_with_len_impl(source, runtime_strlen(source)) can Memory.Allocate, Abort.Panic
    return count


def frontend_lexer_token_count_with_len_impl(source: any u8&, source_len: usize) -> i64 can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        arena: mutable any Arena& = (&frontend_lexer_bench_arena).cast[mutable any Arena&]
        arena_reset(arena)
        return frontend_count_tokens_with_len(arena, "frontend_lexer_bench.llcontext".cast[any u8&], source, source_len).i64()


def frontend_lexer_token_checksum_impl(source: any u8&) -> u64 can[Memory.Allocate, Abort.Panic]:
    checksum: u64 = frontend_lexer_token_checksum_with_len_impl(source, runtime_strlen(source)) can Memory.Allocate, Abort.Panic
    return checksum


def frontend_lexer_token_checksum_with_len_impl(source: any u8&, source_len: usize) -> u64 can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        arena: mutable any Arena& = (&frontend_lexer_bench_arena).cast[mutable any Arena&]
        arena_reset(arena)
        tokens: darray[FrontendToken] = frontend_tokenize_with_len(arena, "frontend_lexer_bench.llcontext".cast[any u8&], source, source_len)
        return frontend_token_checksum(tokens)


def frontend_lexer_token_kind_at_impl(source: any u8&, index: usize) -> u64 can[Memory.Allocate, Abort.Panic]:
    kind: u64 = frontend_lexer_token_kind_at_with_len_impl(source, runtime_strlen(source), index) can Memory.Allocate, Abort.Panic
    return kind


def frontend_lexer_token_kind_at_with_len_impl(source: any u8&, source_len: usize, index: usize) -> u64 can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        arena: mutable any Arena& = (&frontend_lexer_bench_arena).cast[mutable any Arena&]
        arena_reset(arena)
        tokens: darray[FrontendToken] = frontend_tokenize_with_len(arena, "frontend_lexer_bench.llcontext".cast[any u8&], source, source_len)
        if index >= tokens.count:
            return 0
        return frontend_token_kind_code(tokens[index].kind)


def frontend_lexer_copy_token_kinds_impl(source: any u8&, out: mutable any u64&, capacity: usize) -> usize can[Memory.Allocate, Abort.Panic]:
    count: usize = frontend_lexer_copy_token_kinds_with_len_impl(source, runtime_strlen(source), out, capacity) can Memory.Allocate, Abort.Panic
    return count


def frontend_lexer_copy_token_kinds_with_len_impl(source: any u8&, source_len: usize, out: mutable any u64&, capacity: usize) -> usize can[Memory.Allocate, Abort.Panic]:
    can Memory.Allocate, Abort.Panic:
        arena: mutable any Arena& = (&frontend_lexer_bench_arena).cast[mutable any Arena&]
        arena_reset(arena)
        tokens: darray[FrontendToken] = frontend_tokenize_with_len(arena, "frontend_lexer_bench.llcontext".cast[any u8&], source, source_len)
        if capacity < tokens.count:
            return tokens.count

        for i in 0..<tokens.count:
            out[i] <- frontend_token_kind_code(tokens[i].kind)
        return tokens.count

export func frontend_lexer_parity_suite() -> int = frontend_lexer_parity_suite_impl
export func frontend_lexer_token_count(source: any u8&) -> i64 = frontend_lexer_token_count_impl
export func frontend_lexer_token_count_with_len(source: any u8&, source_len: usize) -> i64 = frontend_lexer_token_count_with_len_impl
export func frontend_lexer_token_checksum(source: any u8&) -> u64 = frontend_lexer_token_checksum_impl
export func frontend_lexer_token_checksum_with_len(source: any u8&, source_len: usize) -> u64 = frontend_lexer_token_checksum_with_len_impl
export func frontend_lexer_token_kind_at(source: any u8&, index: usize) -> u64 = frontend_lexer_token_kind_at_impl
export func frontend_lexer_token_kind_at_with_len(source: any u8&, source_len: usize, index: usize) -> u64 = frontend_lexer_token_kind_at_with_len_impl
export func frontend_lexer_copy_token_kinds(source: any u8&, out: mutable any u64&, capacity: usize) -> usize = frontend_lexer_copy_token_kinds_impl
export func frontend_lexer_copy_token_kinds_with_len(source: any u8&, source_len: usize, out: mutable any u64&, capacity: usize) -> usize = frontend_lexer_copy_token_kinds_with_len_impl
