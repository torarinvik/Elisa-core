def ptr_bits(ptr: u8&) -> uintptr:
    return ptr.uintptr()


def bits_ptr(bits: uintptr) -> u8&:
    return bits.cast[u8&]


def advance_raw(ptr: void&, offset: usize) -> void&:
    return (ptr.cast[u8&] + offset).cast[void&]
