def reduce_sum_u64_impl(data: u64&, len: usize, rounds: usize, bias: u64) -> u64:
    acc: mutable u64 = bias

    for round in 0..<rounds:
        partial: mutable u64 = round.u64()
        for i in 0..<len:
            partial += data[i]
        acc += partial

    return acc

export func llctx_reduce_sum_u64(data: u64&, len: usize, rounds: usize, bias: u64) -> u64 = reduce_sum_u64_impl
